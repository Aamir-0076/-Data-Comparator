# -Data-Comparator
The Data Comparator is a robust Python-based tool designed to simplify validating and comparing data from nested JSON files with data stored in relational databases. Its primary role is to detect and report inconsistencies, ensuring accurate and reliable synchronization between different data systems.
